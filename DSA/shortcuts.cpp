// to keep relative order of equal elements
// {
//      vi ord(n);
//         iota(all(ord), 0);
//         stable_sort(all(ord), [&](int i, int j)
//                     { return a[i] > a[j]; });
// }
